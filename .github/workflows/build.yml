name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches: [ "main", "test" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Cache Maven packages on GitHub Actions runner
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Check context
        run: ls -larth ${{ github.workspace }}

      # Pre-download dependencies on the host
      - name: Download Maven dependencies
        run: mvn dependency:go-offline -B -Dmaven.repo.local=${{ github.workspace }}/.m2/repository

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build the image with BuildKit cache mount
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: my-app:latest
          # Use GitHub Actions cache backend for BuildKit
          cache-from: type=gha,scope=build-${{ github.ref_name }}
          cache-to: type=gha,scope=build-${{ github.ref_name }},mode=max